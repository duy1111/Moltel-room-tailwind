const initState = {
    isLoggedIn: false,
    token:null
}

const authReducers  = (state=initState,action ) =>{
    switch(action.type){
        default:
            return state
    }
}

export default authReducers;